{
  "name": "@ux-assistant/ux-review-mcp",
  "version": "0.1.0",
  "description": "UX review MCP server with scoring library",
  "license": "Apache-2.0",
  "private": true,
  "packageManager": "pnpm@10.34.1",
  "type": "module",
  "main": "./dist/server.js",
  "types": "./dist/server.d.ts",
  "bin": {
    "ux-review-mcp": "./bin/ux-review-mcp"
  },
  "workspaces": [
    "scoring"
  ],
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3"
    ]
  },
  "scripts": {
    "build": "pnpm run build:core",
    "build:core": "pnpm --filter @ux-assistant/scoring build && tsc",
    "build:xmcp": "pnpm run build && xmcp build --vercel && node ./scripts/fix-vercel-function-package.cjs",
    "dev": "xmcp dev",
    "doctor": "node ./scripts/doctor.mjs",
    "typecheck": "tsc --noEmit",
    "test": "pnpm run build && node --test dist/contract.test.js dist/storage.test.js dist/validation-engine.test.js dist/report.test.js dist/server-tools.test.js dist/validators/validators.test.js",
    "start": "node dist/cli.js",
    "auto-update:enable": "sh ./scripts/enable-auto-update.sh",
    "auto-update:pull": "sh ./scripts/auto-update.sh main",
    "verify:contract": "pnpm run build && node dist/contract-check.js",
    "verify:scenarios": "pnpm run build && node dist/scenario-check.js",
    "verify:phase8": "pnpm run verify:contract && pnpm run verify:scenarios"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@ux-assistant/scoring": "workspace:*",
    "better-sqlite3": "^11.7.0",
    "playwright-core": "^1.61.1",
    "xmcp": "^0.5.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^22.15.30",
    "typescript": "^6.0.3"
  }
}
